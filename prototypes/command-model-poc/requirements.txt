# core harness (dataset, validation, eval)
lark>=1.1

# intent+slots model (Brevitas QAT) — the from-scratch conv net
torch>=2.2
brevitas>=0.11

# pretrained-encoder intent+slots model (#1847). Fast tokenizers required:
# the subword<->word alignment in model/data_encoder.py needs word_ids().
transformers>=4.44

# optional: scoring a generative GGUF teacher on the M1
# llama-cpp-python>=0.2

# optional: keep train_colab.py <-> .ipynb in sync
# jupytext>=1.16
