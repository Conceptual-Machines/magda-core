.venv/
runs/
__pycache__/
*.pyc
.teacher_cache.json

# Pretrained-encoder checkpoints (#1847): 0.5-1.1 GB each. The conv net's
# model/artifacts/ is 248K and stays tracked; these are reproduced by
# `python -m model.train_encoder --preset <name>`.
model/artifacts_encoder*/

# ONNX export output: the fp32 graph is ~736 MB and tokenizer.json ~9 MB.
# Reproduce with `python -m model.export_onnx`.
model/artifacts_onnx/
